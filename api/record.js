const pool = require("../database");

module.exports = {
    getRecordByUsername
}

/**
 * 
 * @param {String} username 
 * @param {String} password 
 */
async function getRecordByUsername(username, password){
    
}